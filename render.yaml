services:
  # Todo List Web Application
  - type: web
    name: todo-list-hackathon-phase-1
    runtime: python
    buildCommand: pip install --no-cache-dir -e ".[dev]"
    startCommand: python -m todo_app.web
    envVars:
      - key: PYTHONPATH
        value: /app/src
    healthCheckPath: /